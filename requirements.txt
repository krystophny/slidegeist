# Core dependencies (exact versions for reproducibility)
numpy==2.3.4
opencv-python==4.12.0.88
yt-dlp==2024.12.23
pytesseract==0.3.13
Pillow==11.3.0
tqdm==4.67.1
psutil==7.0.0

# Inference is intentionally out of process. Point Slidegeist at compatible
# Whisper and multimodal OpenAI-style HTTP endpoints; no Torch stack is needed.
